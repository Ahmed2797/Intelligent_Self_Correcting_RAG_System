# Intelligent_Self_Correcting_RAG_System
 RAG system that intelligently decides when retrieval is needed, evaluates document relevance, generates context-grounded answers, verifies factual support, revises unsupported responses, and rewrites queries when retrieved information is not useful. This iterative workflow helps reduce hallucinations and improve answer accuracy and reliability.
